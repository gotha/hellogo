apiVersion: v1
kind: Service
metadata:
  name: {{ template "hellogo.fullname" . }}
  labels:
    app: {{ template "hellogo.name" . }}
    chart: {{ template "hellogo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hellogo.name" . }}
    release: {{ .Release.Name }}
